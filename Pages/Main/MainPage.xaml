<Page
    x:Class="MH3GHDSaveEditor.Pages.Main.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MH3GHDSaveEditor.Pages.Main"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sd="clr-namespace:MH3GHDSaveEditor.SaveDataModel"
    Title="MainPage"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Page.DataContext>
        <sd:SaveDataModel />
    </Page.DataContext>
    <GroupBox Header="基本信息">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Label>名字</Label>
            <TextBox
                x:Name="CharacterName"
                Grid.Column="1"
                Text="{Binding Name, Mode=TwoWay}" />

            <Label Grid.Row="1">性别</Label>
            <ComboBox
                x:Name="CharacterSex"
                Grid.Row="1"
                Grid.Column="1"
                SelectedIndex="{Binding Sex, Mode=TwoWay}">
                <ComboBoxItem Content="男" />
                <ComboBoxItem Content="女" />
            </ComboBox>

            <Label Grid.Row="2">金钱</Label>
            <TextBox
                x:Name="CharacterZenny"
                Grid.Row="2"
                Grid.Column="1"
                Text="{Binding Zenny, Mode=TwoWay}" />

            <Label Grid.Row="3">资源点数</Label>
            <TextBox
                x:Name="CharacterPoint"
                Grid.Row="3"
                Grid.Column="1"
                Text="{Binding Point, Mode=TwoWay}" />

            <Button
                Grid.Row="5"
                Margin="5"
                VerticalAlignment="Bottom"
                Click="OpenSaveFile_Click"
                Content="打开存档" />
            <Button
                Grid.Row="5"
                Grid.Column="1"
                Margin="5"
                Click="CloseSaveFile_Click"
                Content="保存并关闭存档" />
        </Grid>
    </GroupBox>
</Page>
